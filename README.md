# Codespace
const extensions = [   { id: "prettier", name: "Prettier", desc: "Code formatter", icon: "🔌" },   { id: "eslint", name: "ESLint", desc: "JS linting", icon: "🎨" } ];const state = {   installed: new Set(),   filter: "all" };
# Extension Marketplace UI

Static UI clone of a VS Code extension marketplace.

## Run

Open in GitHub Codespaces or VS Code Live Server.

## Stack
- HTML
- CSS
- Optional JS<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Extension Marketplace</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>

<div class="container">
    <!-- (your full body content stays unchanged) -->
</div>

</body>
</html># VS Code Marketplace Clone

AI-powered extension marketplace with Firebase + React + recommendation engine.

## Run in Codespaces

Auto-start enabled:
- React frontend runs on port 3000
- Firebase functions on port 5001

## Features
- Extension catalog
- AI recommendation scoring
- Firebase backend
- Extensible architecture
